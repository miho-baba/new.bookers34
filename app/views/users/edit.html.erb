

<p id="notice"></p>
  <div class="container">
  <div class="row">
<div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
 <% if @user.errors.any? %>
  <%= @user.errors.count %>error prohibited this user from being saved
  <ul>
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
 <h1>User info</h1>
<%= form_with model: @user, url: user_path(@user.id), get: :patch do |f| %>
  <div class="field">
    <label for="user_name">Name</label>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <p><label for="user_image">Image</label>選択されていません</p>
    <%= f.submit 'ファイルを選択' %>
  </div>
  <div class="field">
    <label for="user_Opinion">Introduction</label>
    <%= f.text_area :introduction %>
  </div>

  <div class="actions">

   <%= f.submit 'Update User' %>
 </div>
 <% end %>
    </div>
  </div>
</div>

<div class="container mt-5">
    <div class="row">
    <div class="mx-auto">
      <p>
        CopyRight Infratop.inc
      </p>
    </div>
  </div>
</div>
