
 <h1>Editing Book</h1>
<%= form_with model: @book, url: book_path(@book.id), get: :patch do |f| %>

 <% if @book.errors.any? %>
  <%= @book.errors.count %>error prohibited this book from being saved
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

 <div class="field">
    <label for="book_title">Title</label>
 <%= f.text_field :title %>
</div>

  <div class="field">
    <label for="book_Opinion">Body</label>
 <%= f.text_area :body %>
  </div>

  <div class="actions">

   <%= f.submit 'Update Book' %>
 </div>
 <% end %>


<%= link_to 'Show', homes_edit_path(@book.id) %>

<%= link_to 'Back', books_path %>

