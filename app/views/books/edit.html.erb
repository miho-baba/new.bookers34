<p id="notice"></p>
  <div class="container">
  <div class="row">
  <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  <% if @book.errors.any? %>
  <%= @book.errors.count %>error prohibited this book from being saved
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
  <% end %>
  </ul>
  <% end %>
  <h1>Editing Book</h1>
  <%= form_with model: @book, url: book_path(@book.id), get: :patch do |f| %>
  <div class="form-group">
    <label for="book_title">Title</label>
    <input class="form-control book_title" type="text" value="" name="book[title]" id="book_title">
  </div>

  <div class="form-group">
    <label for="book_Opinion">Body</label>
    <textarea class="form-control book_body" name="book[body]" id="book_Opinion"></textarea>
  </div>

  <div class="form-group">
    <input type="submit" name="commit" value="Update Book" class="btn btn-success" data-disable-with="Update Book">
  </div>
  <% end %>


  <%= link_to 'Show', book_path(@book.id) %>
   |
  <%= link_to 'Back', books_path %>
    </div>
  </div>
</div>

  <footer class="bg-white text-black pt-5 pb-4">
      <div class="container">
      <div class="row no-gutters">
        <div class="col-md-12">
        <div class="text-black mt-2 mb-3">
        <ul class="p-0 list-unstyled">
        <li class="text-black-100 text-center">
         CopyRight Infratop.inc
        </li>
            </ul>
          </div>
        </div>
       </div>
      </div>
    </footer>